import javax.swing.*;
import java.awt.*;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) {
/*
        //JFrame = GUI windows to add componenets too

        JFrame frame = new JFrame(); // creates a frame
        frame.setTitle("JFrame title goes here"); // sets title for a frame
        //frame.setDefaultCloseOperation(JFrame.DO_NOTHING_ON_CLOSE);// When x button is clicked it ignores it and still runs the app.
        //frame.setDefaultCloseOperation(JFrame.HIDE_ON_CLOSE); // Acts as it is closed but still runs in the background
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exit out of application
        frame.setResizable(false);// prevents frame from being resized
        frame.setSize(420,420); // sets the x-dimension and y-dimension of the frame
        frame.setVisible(true);//make frame visible

        ImageIcon image =  new ImageIcon("Iron Man.png");// Creates an image icon
        frame.setIconImage(image.getImage());// change icon of frame
        //frame.getContentPane().setBackground(Color.green); // change color of background
        //frame.getContentPane().setBackground(new Color(0,0,0)); // change color of background to black
        //frame.getContentPane().setBackground(new Color(255,255,255)); // change color of background to white
        //frame.getContentPane().setBackground(new Color(255,0,0)); // change color of background to red
        //frame.getContentPane().setBackground(new Color(0,255,0)); // change color of background to green
        //frame.getContentPane().setBackground(new Color(0,0,255)); // change color of background to blue
        frame.getContentPane().setBackground(new Color(123,50,250)); // change color of background to twitch purple
        */

        MyFrame myFrame = new MyFrame(); // calling your class within a variable, best for changes or...
        //new MyFrame(); // Just simply calls the class.

    }
}